# Keep the build context to sources. Everything here is either rebuilt inside the
# image, secret, or irrelevant to a build — and a smaller context uploads faster.
target/
build/
uploads/
.git/
.github/
.mvn/
mvnw
mvnw.cmd
.env
.env.*
!.env.example
*.md
